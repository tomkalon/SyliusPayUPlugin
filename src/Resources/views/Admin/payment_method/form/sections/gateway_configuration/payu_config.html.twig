{% set form = hookable_metadata.context.form %}

{% if form.gatewayConfig.config ?? false %}
    <div class="row">
        <div class="col-12 col-md-6">
            {{ form_row(form.gatewayConfig.config, sylius_test_form_attribute('gateway-configuration')) }}
        </div>
    </div>
{% endif %}
